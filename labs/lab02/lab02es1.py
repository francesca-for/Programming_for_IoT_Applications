import cherrypy
import json

class WebCalculator:
    exposed = True
    def GET(sefl, *uri, **param):
        ...
        self.add(operation, operands)
    
    def add(self, operation, operands):
        ...

if __name__ == "main":

